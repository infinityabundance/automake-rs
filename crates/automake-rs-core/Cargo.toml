[package]
name = "automake-rs-core"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "Core Automake semantic engine: Makefile.am parser, Makefile.in generator, aclocal macro discovery, primaries, rule synthesis, dependency tracking."
readme = "README.md"

[dependencies]
m4-rs-core = "0.1"
autoconf-rs-core = "0.1"
regex = "1"
serde = { workspace = true }
serde_json = { workspace = true }
rowan = "0.15"

[dev-dependencies]
tempfile = "3"
